package com.metlife.legacy;

public class DatabaseConnection {
    public boolean checkLoyaltyStatus(String policyNumber) {
        // policyNumber 로 대상 조회

        // 고객이 로얄티 고객인지 검증
        throw new RuntimeException("🚨 실제 DB 연결 필요! (테스트 환경에서는 실행 불가)");
    }
}
